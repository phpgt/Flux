{
	"name": "@phpgt/flux",
	"exports": "./src/main.es6",
	"type": "module",
	"dependencies": {
		"esbuild": "^0.25.2"
	},
	"devDependencies": {
		"vitest": "^4.1.2",
		"jsdom": "^24.0.0"
	},
	"scripts": {
		"build": "esbuild src/main.es6 --bundle --outfile=dist/flux.js --format=esm --platform=browser --loader:.es6=js",
		"test": "vitest"
	},
	"vitest": {
		"environment": "jsdom"
	}
}
